#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx commitlint --edit "$1" || (echo 'Custom error message: Your commit message does not follow the conventional commit style. Please use one of the following types: feat, fix, docs, style, refactor, perf, test, chore.' && exit 1)
